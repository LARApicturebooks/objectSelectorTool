import { Image, Container, Row, Col, Button } from "react-bootstrap";
import { useEffect, useState } from "react";
import { VariableContext } from "./Pages";

export default function Saving() {
  return (
    <h3 className="savingContainer">saving...</h3>
  );
}
